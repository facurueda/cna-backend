meta {
  name: Create
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/clip-collections
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  "name": "Oficial",
  "description": "Colección principal de clips formativos."
}
