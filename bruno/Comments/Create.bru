meta {
  name: Create
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/comments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  "clipId": "{{clipId}}",
  "content": "Para mí es 2 minutos por impacto en la cara."
}
