meta {
  name: Set Role (Admin)
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/auth/role
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  "email": "user@vyro.com",
  "role": "ADMIN"
}
