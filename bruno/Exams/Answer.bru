meta {
  name: Answer
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/exams/{{examId}}/answer
  body: json
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}

body:json {
  "examQuestionId": "{{examQuestionId}}",
  "selectedKeys": ["a"]
}
