meta {
  name: Remove Referee (Admin)
  type: http
  seq: 9
}

delete {
  url: {{baseUrl}}/competitions/{{competitionId}}/referees/{{refereeId1}}
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}
